package com.typesafe.npm.tester;

import akka.actor.ActorSystem;
import akka.japi.Procedure;
import akka.util.Timeout;

import java.util.concurrent.TimeUnit;

public class JavaMain {
    /*public static void main(String[] args) {
        final ActorSystem system = ActorSystem.create("npm-system");
        final Timeout timeout = new Timeout(5, TimeUnit.SECONDS);

        //system.scheduler().scheduleOnce(new Timeout(7, TimeUnit.SECONDS), new Procedure<>());

    }   */
}
